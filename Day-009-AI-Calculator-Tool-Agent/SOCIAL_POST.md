🧰 Day 009/100 — My AI agent used its first real tool.

Today’s build moves beyond classification into the core agent loop:

**decide → call tool → observe → answer**

I built a Calculator Tool Agent with explicit tool arguments, safe AST-based arithmetic (no `eval()`), grounded answers, and automated tests.

Small project. Big architectural step: this is where a chatbot starts becoming an agent.

⭐ Star the repo and follow the path from prompting → tools → RAG → memory → MCP → multi-agent systems → production AI.

#AIAgents #AgenticAI #Python #BuildInPublic
